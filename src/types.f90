module types 
    !types, needs to be integrated properly.
    !to be honest - should maybe just get rid - as if I want to compile this
    !for python I need to stop using it.
    use, intrinsic :: iso_fortran_env, only: f64 => real64
    use, intrinsic :: iso_fortran_env, only: i32 => int32
    implicit none
end module 